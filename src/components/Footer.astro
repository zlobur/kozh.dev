---
import ThemeFooter from '@navfolio/theme-default/components/Footer.astro';
import { getSiteConfig } from '../data/site';

const { profile, site } = await getSiteConfig();
---

<ThemeFooter site={site} profile={profile} />
